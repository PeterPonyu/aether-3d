# CI / Code-level Tests (Aether3D)

Reserved for fast, CI-friendly tests.

Most code-level tests live in `tests/` (run via `pytest`).

See the root `scripts/README.md` for the full three-tier organization (CI vs Data Flow vs E2E).
